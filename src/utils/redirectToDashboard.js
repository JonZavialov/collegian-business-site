function redirectToDashboard(title){
    // check if title exists within json
    // if so, redirect to new URL
    // otherwise returh
    console.log(`redirecting to ${title}`)
}

export default redirectToDashboard;